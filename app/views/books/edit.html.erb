<p id="notice">
 <%= flash[:notice] %>
</p>

<h1><strong>Editing Book</strong></h1>


<% if @editbook.errors.any? %>
  <%= @editbook.errors.count %>errors prohibited this book from being saved:
  <ul>
    <%= @editbook.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @editbook, url: book_path(@editbook.id), method: :patch do |f| %>
  <div class="field">
    <label for="book_title">
          本のタイトル
    </label>
       <%= f.text_field :title %>
  </div>

  <div class="field">
    <label for="book_title">
          感想
    </label>
       <%= f.text_area :body %>
  </div>

  <div class="actions">
   <%= f.submit 'Update Book' %>
  </div>
<% end %>

<%= link_to "Show",book_path(@editbook.id) %>
|
<%= link_to "Back", "/books" %>

